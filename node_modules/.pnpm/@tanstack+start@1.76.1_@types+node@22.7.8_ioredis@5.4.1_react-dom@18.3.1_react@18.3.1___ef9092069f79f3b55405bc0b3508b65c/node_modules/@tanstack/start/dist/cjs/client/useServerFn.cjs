"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const reactRouter = require("@tanstack/react-router");
function useServerFn(serverFn) {
  const router = reactRouter.useRouter();
  return async (...args) => {
    try {
      const res = await serverFn(...args);
      if (reactRouter.isRedirect(res)) {
        throw res;
      }
      return res;
    } catch (err) {
      if (reactRouter.isRedirect(err)) {
        router.navigate(
          router.resolveRedirect({
            ...err,
            _fromLocation: router.state.location
          })
        );
      }
      throw err;
    }
  };
}
exports.useServerFn = useServerFn;
//# sourceMappingURL=useServerFn.cjs.map
