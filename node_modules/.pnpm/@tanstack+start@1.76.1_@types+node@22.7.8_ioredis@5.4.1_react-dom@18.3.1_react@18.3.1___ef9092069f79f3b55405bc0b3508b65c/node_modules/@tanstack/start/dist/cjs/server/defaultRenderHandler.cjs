"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("react/jsx-runtime");
const ReactDOMServer = require("react-dom/server");
const StartServer = require("./StartServer.cjs");
const defaultRenderHandler = ({
  router,
  responseHeaders
}) => {
  try {
    const html = ReactDOMServer.renderToString(/* @__PURE__ */ jsxRuntime.jsx(StartServer.StartServer, { router }));
    return new Response(`<!DOCTYPE html>${html}`, {
      status: router.state.statusCode,
      headers: responseHeaders
    });
  } catch (error) {
    console.error("Render to string error:", error);
    return new Response("Internal Server Error", {
      status: 500,
      headers: responseHeaders
    });
  }
};
exports.defaultRenderHandler = defaultRenderHandler;
//# sourceMappingURL=defaultRenderHandler.cjs.map
