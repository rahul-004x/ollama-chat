import { EventHandler } from 'vinxi/http';
import { AnyRouter, Manifest } from '@tanstack/react-router';
import { HandlerCallback } from './defaultStreamHandler.js';
export type CustomizeStartHandler<TRouter extends AnyRouter> = (cb: HandlerCallback<TRouter>) => EventHandler;
export declare function createStartHandler<TRouter extends AnyRouter>({ createRouter, getRouterManifest, }: {
    createRouter: () => TRouter;
    getRouterManifest?: () => Manifest;
}): CustomizeStartHandler<TRouter>;
